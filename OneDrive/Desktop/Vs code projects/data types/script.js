// var a = prompt("Matn yozing").trim();
// console.log(a.split(a.charAt(a.length-1; 

var a = prompt("soz kiriting").trim();

if (a) {
    if(a[0] == a[0].toLowerCase()){
        alert(a[0].toUpperCase() + a.slice(1))
    }
    else {
        alert(a[0].toLowerCase() + a.slice(1));
    }
}
// var res = a.charAt(0).toUpperCase() + a.slice(1).toLowerCase();
// alert(res);